<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.szu.admin.mapper.UserMapper">

    <resultMap id="UserMap" type="com.szu.admin.domain.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="listUsers" resultMap="UserMap">
        SELECT * FROM user
        WHERE 1=1
        <if test="isDeleted != null">
            AND is_deleted = #{isDeleted}
        </if>
        <if test="role != null">
            AND role = #{role}
        </if>
        ORDER BY id DESC
    </select>

    <select id="findByUsername" resultMap="UserMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultMap="UserMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findByPhone" resultMap="UserMap">
        SELECT * FROM user WHERE phone = #{phone}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, nickname, avatar, phone, status, is_deleted, role, created_at, updated_at)
        VALUES (#{username}, #{password}, #{nickname}, #{avatar}, #{phone}, #{status}, #{isDeleted}, #{role}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
            <if test="isDeleted != null">is_deleted = #{isDeleted},</if>
            <if test="role != null">role = #{role},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="countUsers" resultType="long">
        SELECT COUNT(1) FROM user
        WHERE 1=1
        <if test="isDeleted != null">
            AND is_deleted = #{isDeleted}
        </if>
        <if test="role != null">
            AND role = #{role}
        </if>
    </select>

</mapper>
