<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.szu.admin.mapper.AdminUserMapper">

    <resultMap id="AdminUserMap" type="com.szu.admin.domain.AdminUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="listAdminUsers" resultMap="AdminUserMap">
        SELECT * FROM admin_user
        WHERE 1=1
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY id DESC
    </select>

    <select id="findAdminUserByUsername" resultMap="AdminUserMap">
        SELECT * FROM admin_user WHERE username = #{username}
    </select>

    <select id="findAdminUserById" resultMap="AdminUserMap">
        SELECT * FROM admin_user WHERE id = #{id}
    </select>

    <select id="findAdminUserByPhone" resultMap="AdminUserMap">
        SELECT * FROM admin_user WHERE phone = #{phone}
    </select>

    <insert id="insertAdminUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin_user (username, password, nickname, avatar, phone, status, created_at, updated_at)
        VALUES (#{username}, #{password}, #{nickname}, #{avatar}, #{phone}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateAdminUser">
        UPDATE admin_user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="countAdminUsers" resultType="long">
        SELECT COUNT(1) FROM admin_user
        WHERE 1=1
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

</mapper>
